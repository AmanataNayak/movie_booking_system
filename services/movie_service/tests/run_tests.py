import sys
import pytest

# This command explicitly starts pytest and tells it to discover
# and run all tests in the current directory and subdirectories.
if __name__ == "__main__":
    sys.exit(pytest.main())